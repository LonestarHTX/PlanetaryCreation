# Unreal Engine build output and caches
Binaries/
DerivedDataCache/
Intermediate/
Saved/
Build/
Builds/

# Local-only plugins or temp content (uncomment if desired)
# Plugins/Developer/
# Content/Developers/

# IDE and tooling settings
.vs/
.vscode/
.idea/
*.VC.db
*.VC.opendb
*.suo
*.user
*.userosscache
*.sln.docstates

# Compiler and linker artifacts
*.obj
*.pdb
*.ipch
*.db
*.ilk
*.pch

# Logs and temporary files
*.log
*.tlog
*.cache
*.tmp
*.temp
*_tmp.*

# OS-specific files
.DS_Store
Thumbs.db

# Optional: uncomment to use Git LFS for large binary assets
# *.uasset filter=lfs diff=lfs merge=lfs -text
# *.umap filter=lfs diff=lfs merge=lfs -text
*_key.txt

# Stage B SRTM data (binary files - don't commit)
StageB_SRTM90/raw/*.tif
StageB_SRTM90/cropped/*.tif
Content/PlanetaryCreation/Exemplars/COG/*.tif
Content/PlanetaryCreation/Exemplars/PNG16/*.png

# BUT keep manifests and documentation
!StageB_SRTM90/metadata/*.json
!Content/PlanetaryCreation/Exemplars/*.json
